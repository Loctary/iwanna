import FirebaseContext, { withFirebase, FirebaseType as FirebaseType1 } from './FirebaseHOC';
// import type {  } from './FirebaseHOC';
import Firebase from './Firebase';

export default Firebase;

export { FirebaseContext, withFirebase };
export type FirebaseType = FirebaseType1;
